$('.search').on('ajax:success', function() {
  $('.event-list').html('<%= j render partial: 'list', locals: { events: @events} %>');
  $('.pagination').html('<%= j will_paginate @events, renderer: BootstrapPagination::Rails %>');
});